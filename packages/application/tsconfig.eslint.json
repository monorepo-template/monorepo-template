{
  "extends": "../../tsconfig.eslint.json",
  "include": [".eslintrc.cjs", "**/*", "**/*.json"],

  "compilerOptions": {
    "jsx": "react-jsx",
    "types": ["jest"]
  },

  "exclude": [
    "build",
    "coverage",
    "cypress/coverage",
    "cypress/screenshots",
    "cypress/videos",
    "jest",
    "node_modules",
    "public/**/*.html",
    "public/**/*.ico",
    "public/**/*.txt"
  ]
}
