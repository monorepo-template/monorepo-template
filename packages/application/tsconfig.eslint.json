{
  "extends": "../../tsconfig.eslint.json",
  "include": [".eslintrc.cjs", "**/*"],

  "compilerOptions": {
    "jsx": "react-jsx",
    "types": ["jest"]
  },

  "exclude": [
    "build",
    "coverage",
    "cypress/coverage",
    "cypress/screenshots",
    "cypress/videos",
    "jest",
    "node_modules"
  ]
}
